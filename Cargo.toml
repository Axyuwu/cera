[package]
name = "cera"
version = "0.1.0"
edition = '2021'

[profile.profile]
inherits = "release"
debug = true

[dependencies]
derive_more = { version = "2.1.0", features = ["from"] }
nom = "8.0.0"

